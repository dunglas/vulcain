package gateway

/*import "strings"

func parsePreferHeader(values []string) ([]string, []string) {
	for _, commaSepValues := range values {
		for _, rawValue := range strings.Split(commaSepValues, ",") {
			parts := strings.Split(strings.Trim(rawValue, " "), "=")
			if (parts[0] != "") {

			}
		}
	}
}*/
